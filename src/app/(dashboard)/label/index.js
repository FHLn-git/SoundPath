/**
 * Dashboard route group: Label app at /app/label/[...slug] or (subdomain) /label/[...slug].
 * Uses MobileLayout + Sidebar. Do not render MarketingLayout here.
 */
// Route components are imported in App.jsx (Launchpad, Dashboard, etc.)
