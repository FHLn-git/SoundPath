/**
 * Dashboard route group: Venue app at /app/venue or (subdomain) /venue.
 * Full viewport; no Label sidebar. Do not render MarketingLayout here.
 */
export { default as VenueApp } from '../../../pages/VenueApp'
